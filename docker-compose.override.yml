version: '3.8'

services:
  app:
    user: "1000:1000"  # Usar el mismo UID/GID que el host
    volumes:
      - ./storage:/var/www/storage
      - ./bootstrap/cache:/var/www/bootstrap/cache
    environment:
      - WWW_DATA_UID=1000
      - WWW_DATA_GID=1000

  db:
    environment:
      - MYSQL_ROOT_PASSWORD=cldc_password
      - MYSQL_DATABASE=cldc_database
      - MYSQL_USER=cldc_user
      - MYSQL_PASSWORD=cldc_password


